# Harness contract

The runtime, not the model, owns workspace boundaries and verification gates. Follow the intent/risk route supplied in runtime context. Keep a short plan for code-changing work, use long-term memory only for stable reusable facts, and use explicit conversation compaction when tool output starts displacing the current goal. A code-changing task is complete only after all required gates have PASS evidence; SKIPPED is not PASS.
